services:
  prod:
    build:
      context: ./prod
    restart: always
    ports:
      - 8080:8080
      - 4000:4000
    environment:
      CATALINA_TMPDIR: /data
      CATALINA_OPTS: "-Xmx10g"
      JAVA_OPTS: >-
        -XX:+UseConcMarkSweepGC
        -Dorg.voyanttools.server.storage=file-per-corpus
        -javaagent:/glowroot/glowroot.jar
        -Dglowroot.data.dir=/data/glowroot-data
    volumes:
     - /data/:/data
  dev:
    build:
      context: ./dev
    restart: always
    healthcheck:
      test: curl -f http://localhost:8080/resources/ext/6.2.0/charts-all.css || exit 1
      interval: 30s
      timeout: 10s
      retries: 2
    ports:
      - 8080:8080
      - 4000:4000
    environment:
      CATALINA_TMPDIR: /data
      CATALINA_OPTS: "-Xmx10g"
      JAVA_OPTS: >-
        -XX:+UseConcMarkSweepGC
        -Dorg.voyanttools.server.storage=file-per-corpus
        -javaagent:/glowroot/glowroot.jar
        -Dglowroot.data.dir=/data/glowroot-data
    volumes:
     - /data/:/data
